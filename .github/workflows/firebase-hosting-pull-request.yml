# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on workflow dispatch (Manually)
on:
  issue_comment:
    types: [created, edited]
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    if: github.event.comment.body == 'OK to test'
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PENCILPROJECT_CDA84 }}'
          projectId: pencilproject-cda84
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
